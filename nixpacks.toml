# Archivo para configurar el proveedor Nixpacks en Railway
[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg-full", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip3 install --upgrade yt-dlp", "npm ci"]

[start]
cmd = "node index.js"
